using UnityEngine;

namespace Game.Monster
{
    public class MonsterAI : MonoBehaviour
    {
        void Start()
        {
            Debug.Log(gameObject.name + " AI initialized. Staying idle for now.");
        }

        // Future AI logic will go here (e.g., Update method for behavior)
    }
}
